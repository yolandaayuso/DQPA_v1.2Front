import { Base, Type } from "./type";
import { ISchedulableRender } from "./ISchedulableRender";
/**
 * @hidden
 */
export declare class SchedulableRenderWrapper extends Base implements ISchedulableRender {
    static $t: Type;
    constructor(a: ISchedulableRender);
    private a;
    private get b();
    get d(): boolean;
    get isDirty(): boolean;
    set isDirty(a: boolean);
    undirty(a: boolean): void;
    get index(): number;
    postRender(): void;
    preRender(): void;
    isValid(): boolean;
    equals(a: any): boolean;
}

import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { CalloutAnnotation as CalloutAnnotation_internal } from './CalloutAnnotation';
import { IgrCalloutAnnotation as IgrCalloutAnnotation } from './igr-callout-annotation';
var IgrCalloutAnnotationCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrCalloutAnnotationCollection, _super);
    function IgrCalloutAnnotationCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrCalloutAnnotation.$type) {
            IgrCalloutAnnotation.$type = markType(IgrCalloutAnnotation, "IgrCalloutAnnotation");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrCalloutAnnotationCollection.prototype._createInnerColl = function () {
        if (!IgrCalloutAnnotation.$type) {
            IgrCalloutAnnotation.$type = markType(IgrCalloutAnnotation, "IgrCalloutAnnotation");
        }
        var coll = new SyncableObservableCollection$2(IgrCalloutAnnotation.$type, CalloutAnnotation_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new IgrCalloutAnnotation();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrCalloutAnnotationCollection;
}(IgCollection));
export { IgrCalloutAnnotationCollection };

import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { SeriesLayer as SeriesLayer_internal } from './SeriesLayer';
import { IgrSeriesLayer as IgrSeriesLayer } from './igr-series-layer';
var IgrSeriesLayerCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrSeriesLayerCollection, _super);
    function IgrSeriesLayerCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrSeriesLayer.$type) {
            IgrSeriesLayer.$type = markType(IgrSeriesLayer, "IgrSeriesLayer");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrSeriesLayerCollection.prototype._createInnerColl = function () {
        if (!IgrSeriesLayer.$type) {
            IgrSeriesLayer.$type = markType(IgrSeriesLayer, "IgrSeriesLayer");
        }
        var coll = new SyncableObservableCollection$2(IgrSeriesLayer.$type, SeriesLayer_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new IgrSeriesLayer();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrSeriesLayerCollection;
}(IgCollection));
export { IgrSeriesLayerCollection };

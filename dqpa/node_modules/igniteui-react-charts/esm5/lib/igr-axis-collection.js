import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { Axis as Axis_internal } from './Axis';
import { IgrAxis as IgrAxis } from './igr-axis';
var IgrAxisCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrAxisCollection, _super);
    function IgrAxisCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrAxis.$type) {
            IgrAxis.$type = markType(IgrAxis, "IgrAxis");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrAxisCollection.prototype._createInnerColl = function () {
        if (!IgrAxis.$type) {
            IgrAxis.$type = markType(IgrAxis, "IgrAxis");
        }
        var coll = new SyncableObservableCollection$2(IgrAxis.$type, Axis_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = IgrAxis._createFromInternal(int);
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrAxisCollection;
}(IgCollection));
export { IgrAxisCollection };

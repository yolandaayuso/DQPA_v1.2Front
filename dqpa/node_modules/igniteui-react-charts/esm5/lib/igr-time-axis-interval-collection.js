import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { TimeAxisInterval as TimeAxisInterval_internal } from './TimeAxisInterval';
import { IgrTimeAxisInterval as IgrTimeAxisInterval } from './igr-time-axis-interval';
var IgrTimeAxisIntervalCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrTimeAxisIntervalCollection, _super);
    function IgrTimeAxisIntervalCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrTimeAxisInterval.$type) {
            IgrTimeAxisInterval.$type = markType(IgrTimeAxisInterval, "IgrTimeAxisInterval");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrTimeAxisIntervalCollection.prototype._createInnerColl = function () {
        if (!IgrTimeAxisInterval.$type) {
            IgrTimeAxisInterval.$type = markType(IgrTimeAxisInterval, "IgrTimeAxisInterval");
        }
        var coll = new SyncableObservableCollection$2(IgrTimeAxisInterval.$type, TimeAxisInterval_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new IgrTimeAxisInterval(null);
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrTimeAxisIntervalCollection;
}(IgCollection));
export { IgrTimeAxisIntervalCollection };

import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { TimeAxisBreak as TimeAxisBreak_internal } from './TimeAxisBreak';
import { IgrTimeAxisBreak as IgrTimeAxisBreak } from './igr-time-axis-break';
var IgrTimeAxisBreakCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrTimeAxisBreakCollection, _super);
    function IgrTimeAxisBreakCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrTimeAxisBreak.$type) {
            IgrTimeAxisBreak.$type = markType(IgrTimeAxisBreak, "IgrTimeAxisBreak");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrTimeAxisBreakCollection.prototype._createInnerColl = function () {
        if (!IgrTimeAxisBreak.$type) {
            IgrTimeAxisBreak.$type = markType(IgrTimeAxisBreak, "IgrTimeAxisBreak");
        }
        var coll = new SyncableObservableCollection$2(IgrTimeAxisBreak.$type, TimeAxisBreak_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new IgrTimeAxisBreak(null);
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrTimeAxisBreakCollection;
}(IgCollection));
export { IgrTimeAxisBreakCollection };

import { __extends } from "tslib";
import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { ChartSelection as ChartSelection_internal } from './ChartSelection';
import { IgrChartSelection as IgrChartSelection } from './igr-chart-selection';
var IgrChartSelectedItemCollection = /** @class */ /*@__PURE__*/ (function (_super) {
    __extends(IgrChartSelectedItemCollection, _super);
    function IgrChartSelectedItemCollection(list) {
        var _this = _super.call(this) || this;
        if (!IgrChartSelection.$type) {
            IgrChartSelection.$type = markType(IgrChartSelection, "IgrChartSelection");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    IgrChartSelectedItemCollection.prototype._createInnerColl = function () {
        if (!IgrChartSelection.$type) {
            IgrChartSelection.$type = markType(IgrChartSelection, "IgrChartSelection");
        }
        var coll = new SyncableObservableCollection$2(IgrChartSelection.$type, ChartSelection_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new IgrChartSelection();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    };
    return IgrChartSelectedItemCollection;
}(IgCollection));
export { IgrChartSelectedItemCollection };

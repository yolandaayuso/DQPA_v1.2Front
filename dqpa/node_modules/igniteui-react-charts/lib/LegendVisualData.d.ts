import { Base, Type } from "igniteui-react-core";
import { LegendVisualDataList } from "./LegendVisualDataList";
import { RectData } from "igniteui-react-core";
/**
 * @hidden
 */
export declare class LegendVisualData extends Base {
    static $t: Type;
    constructor();
    private _items;
    get items(): LegendVisualDataList;
    set items(a: LegendVisualDataList);
    private _viewport;
    get viewport(): RectData;
    set viewport(a: RectData);
    private _width;
    get width(): number;
    set width(a: number);
    private _height;
    get height(): number;
    set height(a: number);
    serialize(): string;
}

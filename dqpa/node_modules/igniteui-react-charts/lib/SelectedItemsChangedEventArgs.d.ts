import { EventArgs, Type } from "igniteui-react-core";
import { ObjectCollection } from "igniteui-react-core";
/**
 * @hidden
 */
export declare class SelectedItemsChangedEventArgs extends EventArgs {
    static $t: Type;
    private _oldItems;
    get oldItems(): ObjectCollection;
    set oldItems(a: ObjectCollection);
    private _newItems;
    get newItems(): ObjectCollection;
    set newItems(a: ObjectCollection);
    private _currentItems;
    get currentItems(): ObjectCollection;
    set currentItems(a: ObjectCollection);
    constructor(a: ObjectCollection, b: ObjectCollection, c: ObjectCollection);
}

import { IgrHorizontalStackedSeriesBase, IIgrHorizontalStackedSeriesBaseProps } from "./igr-horizontal-stacked-series-base";
import { StackedLineSeries } from "./StackedLineSeries";
/**
 * Represents a stacked series, where the points in each series are connected with a line.
*/
export declare class IgrStackedLineSeries<P extends IIgrStackedLineSeriesProps = IIgrStackedLineSeriesProps> extends IgrHorizontalStackedSeriesBase<P> {
    protected createImplementation(): StackedLineSeries;
    /**
                                 * @hidden
                                 */
    get i(): StackedLineSeries;
    constructor(props: P);
    /**
     * Gets whether the current series shows only line shapes.
    */
    get isLineOnly(): boolean;
}
export interface IIgrStackedLineSeriesProps extends IIgrHorizontalStackedSeriesBaseProps {
}

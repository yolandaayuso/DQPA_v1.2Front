import { Base, Type } from "igniteui-react-core";
import { Message } from "./Message";
/**
 * @hidden
 */
export declare class MessageChannel extends Base {
    static $t: Type;
    private c;
    i(a: Message): void;
    e(a: (message: Message) => void): void;
    messageSent: (message: Message) => void;
    h(a: (message: Message) => void): void;
    private a;
    f(a: MessageChannel): void;
    g(): void;
    j(a: Message): void;
    toString(): string;
}

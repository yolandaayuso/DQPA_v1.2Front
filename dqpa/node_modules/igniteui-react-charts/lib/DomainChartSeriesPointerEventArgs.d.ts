import { Base, Point, Type } from "igniteui-react-core";
import { Series } from "./Series";
/**
 * @hidden
 */
export declare class DomainChartSeriesPointerEventArgs extends Base {
    static $t: Type;
    private e;
    constructor(a: Series, b: any, c: Point);
    private _series;
    get series(): Series;
    set series(a: Series);
    private _item;
    get item(): any;
    set item(a: any);
    private _plotAreaPosition;
    get plotAreaPosition(): Point;
    set plotAreaPosition(a: Point);
    get chartPosition(): Point;
    private b;
    get cancelSelection(): boolean;
    set cancelSelection(a: boolean);
}

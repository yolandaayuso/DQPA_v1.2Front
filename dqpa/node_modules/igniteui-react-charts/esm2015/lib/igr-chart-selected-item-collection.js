import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { ChartSelection as ChartSelection_internal } from './ChartSelection';
import { IgrChartSelection as IgrChartSelection } from './igr-chart-selection';
export class IgrChartSelectedItemCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrChartSelection.$type) {
            IgrChartSelection.$type = markType(IgrChartSelection, "IgrChartSelection");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrChartSelection.$type) {
            IgrChartSelection.$type = markType(IgrChartSelection, "IgrChartSelection");
        }
        let coll = new SyncableObservableCollection$2(IgrChartSelection.$type, ChartSelection_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = new IgrChartSelection();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    }
}

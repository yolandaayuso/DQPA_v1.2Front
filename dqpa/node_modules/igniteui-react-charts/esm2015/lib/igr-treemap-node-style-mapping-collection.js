import { IgCollection } from "igniteui-react-core";
import { markType } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { TreemapNodeStyleMapping as TreemapNodeStyleMapping_internal } from './TreemapNodeStyleMapping';
import { IgrTreemapNodeStyleMapping as IgrTreemapNodeStyleMapping } from './igr-treemap-node-style-mapping';
export class IgrTreemapNodeStyleMappingCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrTreemapNodeStyleMapping.$type) {
            IgrTreemapNodeStyleMapping.$type = markType(IgrTreemapNodeStyleMapping, "IgrTreemapNodeStyleMapping");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrTreemapNodeStyleMapping.$type) {
            IgrTreemapNodeStyleMapping.$type = markType(IgrTreemapNodeStyleMapping, "IgrTreemapNodeStyleMapping");
        }
        let coll = new SyncableObservableCollection$2(IgrTreemapNodeStyleMapping.$type, TreemapNodeStyleMapping_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = new IgrTreemapNodeStyleMapping(null);
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    }
}

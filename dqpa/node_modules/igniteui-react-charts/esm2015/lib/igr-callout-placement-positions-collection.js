import { IgCollection } from "igniteui-react-core";
import { SyncableObservableCollection$2 } from "igniteui-react-core";
import { CalloutPlacementPositions_$type } from './CalloutPlacementPositions';
import { ensureEnum } from "igniteui-react-core";
export class IgrCalloutPlacementPositionsCollection extends IgCollection {
    constructor(list) {
        super();
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _ensureOuter(item) {
        return ensureEnum(CalloutPlacementPositions_$type, item);
    }
    _createInnerColl() {
        let coll = new SyncableObservableCollection$2(CalloutPlacementPositions_$type, CalloutPlacementPositions_$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext;
        };
        coll.createFrom = (int) => {
            return int;
        };
        return coll;
    }
}
